using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class First_Choose_Country_UI : MonoBehaviour
{
    public static int Playing_Country = 1;//取りあえず陽帝国にしておく
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
